<div class="tag">
<tag>
	{% for tag in tags -%}
		{% if tag !== 'post' %}
            <a id="{{ tag }}" href="/blog/t/{{ tag }}">#{{ tag }}</a>
		{% endif %}
	{%- endfor %}
</tag> <span class="reading">{{ content | readingTime }} read</span>
</div>